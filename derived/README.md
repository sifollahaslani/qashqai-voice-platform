# Derived Artifacts

This folder is reserved for local AI-derived artifacts such as embeddings, vector indexes, summaries, model inputs, model outputs, and dataset drafts.

These artifacts are ignored by default because they may expose sensitive cultural or personal information even when raw recordings are not present.
